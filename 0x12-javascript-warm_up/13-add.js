#!/usr/bin/node
// task 13 function to add 2 inputs
exports.add = function add (a, b) {
  return (a + b);
};
// no need to name the function is it already named in assign
// so the following will work the same
// export.add = function (a, b){
//   return (a + b);
// }
